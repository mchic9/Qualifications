# Qualifications TP 1
1. Combien de parametres devrait avoir le constructeur ?

Le constructeur devrait avoir 2 paramètres, le code et le libelle.


# Qualifications TP 2

1. Identifer les 3 erreurs 

Les 3 erreurs sont :
   La variable p n'existe pas dans le constructeur.
   Le constructeur ne reçoit aucun paramètre pour le prénom.
   Le nom n'est pas initialiser dans le constructeur.

2. Combien de parametres devrait avoir le constructeur ?

Il de devrait en avoir 3 le nom le prenom et numSecu.

3. Pourquoi il y a t-il un cadenas devant nom ?

Il y a un cadenas devant nom pour indiquer que c'est une variable privée, ce qui signifie qu'elle ne peut pas être accédée directement depuis l'extérieur de la classe.

5. Quels sont les classes qui sont en liaison avec la classe Employe ?

La classe Qualification est en liaison avec la classe Employe, car un employé peut avoir une qualification.

# Qualifications TP 4

1. Quel est l'interet des list 

L'intérêt des listes est de pouvoir stocker plusieurs éléments du même type dans une seule variable, ce qui facilite la gestion et l'organisation des données.

2. Ou est rempli cmbQualification

cmbQualification est rempli dans la méthode initialize() de la classe QualificationController. Cette méthode est appelée lors de l'initialisation du contrôleur et elle remplit le combo box avec les qualifications disponibles.

3. Que cmbQualification.items.clear() fait-il  ?

cmbQualification.items.clear() vide la liste des éléments du combo box cmbQualification. Cela permet de s'assurer que le combo box est vide avant de le remplir à nouveau avec les qualifications disponibles.

4. Pourquoi ajouter cette ligne a chaque fois qu'on ajoute un item ?

Pour s'assurer que le combo box est mis à jour avec les nouvelles qualifications disponibles. Si on ne le fait pas, le combo box pourrait ne pas afficher les nouvelles qualifications ajoutées.
